import React, { Component } from 'react';

const style = {
  minWidth: '100vw',
  minHeight: '100vh',
}

class HomePage extends Component {

  constructor(props) {
    super(props);

    this.state = {
      // open: false,
    };
  }

  render() {
    return (
      <div style={style}>
        
      </div>
    );
  }
}

export default HomePage;
